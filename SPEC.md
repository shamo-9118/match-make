# PickleMatch - コート割り振りアプリ 仕様書

## 1. 概要

ピックルボールなどのラケットスポーツにおいて、物理的なカードや乱数表に依存したコート割りを電子化し、参加者の組み合わせの多様性・休憩回数の公平性を担保するiPad向けアプリ。

---

## 2. 対象ユーザー

- 特定のサークル内での利用（パブリック公開・収益化は現時点で対象外）
- 主にiPadで使用

---

## 3. 技術方針

| 項目             | 内容                                     |
| ---------------- | ---------------------------------------- |
| プラットフォーム | iOS（iPad最適化）                        |
| フレームワーク   | Expo (managed workflow) + React Native   |
| 言語             | TypeScript                               |
| ナビゲーション   | Expo Router                              |
| 状態管理         | Zustand                                  |
| データ永続化     | AsyncStorage + JSON（ローカルのみ）      |
| サーバー・DB     | 使用しない（将来的な拡張は視野に入れる） |

---

## 4. 画面構成

```
TabBar or NavigationStack
├── ユーザー管理画面
├── セッション設定画面
└── コート割り振り画面
```

---

## 5. 機能仕様

### 5.1 ユーザー管理機能

#### 5.1.1 ユーザー登録

- 名前（テキスト入力）
- プロフィール画像（カメラ撮影 or フォトライブラリから選択）
- 登録済みユーザーの一覧表示・編集・削除

#### 5.1.2 ゲーム参加回数

- 各ユーザーの「ゲーム参加回数」「休憩回数」「ペア組み合わせ履歴」「対戦組み合わせ履歴」を内部的に保持
- セッション終了時（練習終了ボタン押下）にリセット

---

### 5.2 セッション設定機能

セッション開始時に以下を設定する：

| 設定項目   | 詳細                                   |
| ---------- | -------------------------------------- |
| コート数   | 使用するコート数（想定: 2〜3コート）   |
| ゲーム形式 | シングルス（1vs1）/ ダブルス（2vs2）   |
| 参加者選択 | 登録済みユーザーから当日の参加者を選択 |

#### 参加者選択UI

- 登録ユーザー一覧から複数選択（想定参加人数: 8〜12人）
- **ゲーム参加回数の降順**でソート表示（多い人が上に来る）
- 選択済みユーザーはチェックマーク等で視覚的に区別

---

### 5.3 コート割り振り機能

#### 5.3.1 割り振りルール

| 項目              | 内容                                                                                     |
| ----------------- | ---------------------------------------------------------------------------------------- |
| 1コートあたり人数 | ダブルス: 4人（2vs2）/ シングルス: 2人（1vs1）                                           |
| 休憩者            | `参加人数 - (コート数 × 1コート人数)` が休憩（0人の場合は全員出場、1人の場合は単独休憩） |
| 公平性            | 各参加者の休憩回数が均等になるよう制御                                                   |
| 多様性            | 過去の対戦・ペア履歴を考慮し、同じ組み合わせを避けるアルゴリズム                         |

#### 5.3.2 割り振りアルゴリズム（概要）

**Step 1: 休憩者の選定**

- 累積休憩回数が最も少ない人を優先的に休憩に割り当て
- 同回数の場合は「直前のラウンドで休憩していない人」を優先して選定
- それでも同条件の場合はランダムで選定（2連続休憩は人数・コート構成上やむを得ない場合のみ）

**Step 2: コートへの割り当て（ダブルスの場合）**

- 残りプレーヤーを各コートへ4人ずつ割り当て
- コートメンバー選定時も「同じコートでプレーした回数」が少ない組み合わせを優先

**Step 3: ペア決め（ダブルスの場合）**

- コート内4人を2vs2に分割
- 過去のペア履歴を参照し、同じペアになった回数が少い組み合わせを優先
- パターンが枯渇した場合は最も少ない頻度のペアをランダムに選択

**スコアリング方針**

- 各候補割り振りに対して「過去の同一ペア回数合計 + 過去の同一コート回数合計」をスコア化
- スコアが最小の割り振りを採用（同値の場合はランダム）

#### 5.3.3 セッション中の参加者変更

- コート割り振り画面に「参加者管理」ボタンを設置
- 参加者の追加・削除が随時可能。次ラウンド生成時に最新の参加者リストを使用

**途中離脱**

- 参加者リストから除外するだけ
- その人のカウントはそのまま保持（次回参加時に引き継ぎ）

**途中参加**

- 参加回数・休憩回数を現参加者の平均値（四捨五入）で初期化
- ペア/対戦履歴は空でスタート
- これにより特別優先・後回しなく自然に回転に組み込まれる

---

#### 5.3.4 割り振り画面 UI

- 各コートのカード表示
  - コート番号
  - チームA（プレーヤー名 + アイコン）
  - チームB（プレーヤー名 + アイコン）
- 休憩プレーヤーの表示エリア
- **「次へ」ボタン**: 次の割り振りを確定・表示。押下時の処理順は以下の通り：
  1. 現ラウンドのカウント（参加回数・休憩回数・ペア/対戦履歴）を即時更新（同期処理）
  2. 事前計算済みの次ラウンドを表示
  3. 更新済みカウントを使って「次の次ラウンド」をバックグラウンドで計算開始
- **「戻る」ボタン**: 過去の割り振りを閲覧するだけ（カウントの巻き戻しはしない）
  - 閲覧中に「次へ」を押すと最新ラウンドへ自動ジャンプし、そのまま次ラウンドへ進む
  - 最新ラウンドまで戻れば通常操作に復帰

#### 5.3.4 コート履歴（戻るスタック）

- 「戻る」でセッション開始まで遡って閲覧が可能（復元機能なし・閲覧のみ）
- 履歴の上限: **50ラウンド**（メモリ対策。8〜12人 × 50ラウンドは現実的に十分）
- 上限超過分は古い方から破棄
- 過去の割り振りを「閲覧中」は、現在の最新状態として確定するまでカウントを変更しない

**現在 vs 過去の視覚的差別化**

- 過去ラウンド閲覧中は画面全体をグレーアウト・バナー表示など、「過去を見ている」ことを明示
- ラウンド番号を常に表示（例: `ラウンド 3 / 7`）
- 「最新に戻る」ボタンを表示し、1タップで最新ラウンドにジャンプできる（過去閲覧中のみ表示）

---

### 5.4 練習終了・リセット機能

- 画面のどこかに「練習終了」ボタンを設置
- 押下時にアラートを表示:
  > 「練習を終了しますか？参加回数・対戦履歴がリセットされます」
  > [終了する] [キャンセル]
- 確定後: 全ユーザーの累積カウント・履歴をリセット、セッションも終了

---

## 6. データモデル（案）

### User

```
id: UUID
name: String
imagePath: String?            // ローカルファイルパス
totalPlayCount: Int           // 累積ゲーム参加回数（セッション内）
totalRestCount: Int           // 累積休憩回数（セッション内）
pairHistory: [UUID: Int]      // ペア相手ID → 同ペア回数
opponentHistory: [UUID: Int]  // 対戦相手ID → 対戦回数
```

### Session

```
id: UUID
date: Date
courtCount: Int
gameFormat: .singles | .doubles
participantIds: [UUID]
rounds: [Round]               // インデックス順に履歴スタック（最大50件）
currentRoundIndex: Int
```

### Round

```
index: Int
courts: [Court]
restingPlayerIds: [UUID]
```

### Court

```
courtNumber: Int
teamA: [UUID]    // ダブルス: 2人 / シングルス: 1人
teamB: [UUID]    // ダブルス: 2人 / シングルス: 1人
```

---

## 7. 将来的な拡張（対応しないが考慮する）

- サーバーへのセッションデータのアップロード・履歴管理
- ペア決めを自動ではなく手動に切り替えるオプション
- 複数スポーツ対応（コート人数のカスタマイズ）

---

## 8. 未確認事項（解消済み）

| #   | 質問               | 回答                                                           |
| --- | ------------------ | -------------------------------------------------------------- |
| 1   | ダブルスのペア表示 | ペアまで決めて表示する。同一ペアが出ないようアルゴリズムで制御 |
| 2   | セッション履歴     | 今日（現セッション）のみ保持                                   |
| 3   | リセットタイミング | 手動「練習終了」ボタン押下時（アラート付き）                   |
| 4   | 規模感             | 2〜3コート、8〜12人                                            |
| 5   | 戻るの範囲         | セッション開始まで（上限50ラウンド）                           |
| 6   | 休憩0人ケース      | 割り切れる場合は全員出場でOK（運用で対処）                     |
| 7   | 単独休憩           | 1人だけ休憩になるケースも許容                                  |
